
namespace UIForia {

    public enum UIFixedUnit {

        Unset = Unit.Unset,
        Pixel = Unit.Pixel,
        Percent = Unit.Percent,
        Em = Unit.Em,
        ViewportWidth = Unit.ViewportWidth,
        ViewportHeight = Unit.ViewportHeight,

        // todo -- remove percent, add app size / block size 
    }

}
