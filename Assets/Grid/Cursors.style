style red {
    BackgroundColor = red;
}

style container {
    LayoutType = Flex;
    FlexLayoutDirection = Column;
    PreferredSize = 300px;
}

style <Group> {
    PreferredSize = 150px;
}

style blue {    
    BackgroundColor = blue;
    [hover] {
        BackgroundColor = yellow;
        Cursor = Cursor1 15;
    }
    [attr:attrName]
}