<?xml version="1.0" encoding="utf-8"?>
<UITemplate>
    <Style>

        style scrollview {
            OverflowX = scroll;
            OverflowY = scroll;
        }

        style scroll-track-vertical {
            PreferredSize = 20px 1psz;
            LayoutBehavior = Ignored;
            AlignX = 100%;
            AlignmentBehaviorX = Parent;
            ZIndex = 1;
            BackgroundColor = darkGrey;
        }

        style scroll-handle-vertical {
            BorderRadius = 50%;
            BackgroundColor = grey;
            PreferredWidth = 20px;
            AlignmentBehaviorY = Parent;
        }

        style scroll-track-horizontal {
            PreferredSize = 0.97psz 20px;
            LayoutBehavior = Ignored;
            AlignmentBehaviorY = Parent;
            AlignY = 100%;
            ZIndex = 1;
            BackgroundColor = darkGrey;
        }

        style scroll-handle-horizontal {
            BorderRadius = 50%;
            BackgroundColor = grey;
            PreferredHeight = 20px;
            AlignmentBehaviorX = Parent;
        }

    </Style>
    
    <Contents style="scrollview">

        <Children x-id="scroll-root" />

        <Group onMouseClick="OnClickVertical($event)" onMouseHover="OnHoverVertical($event)" x-id="scroll-track-vertical" style="scroll-track-vertical">
            <Div onDragCreate="OnCreateVerticalDrag($event)" x-id="scroll-handle-vertical" style="scroll-handle-vertical"/>
        </Group>

        <Group onMouseClick="OnClickHorizontal($event)" onMouseHover="OnHoverHorizontal($event)" x-id="scroll-track-horizontal" style="scroll-track-horizontal">
            <Div onDragCreate="OnCreateHorizontalDrag($event)" x-id="scroll-handle-horizontal" style="scroll-handle-horizontal"/>
        </Group>

    </Contents>
</UITemplate>