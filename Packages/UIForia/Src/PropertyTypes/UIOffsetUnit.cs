
namespace UIForia {

    public enum UIOffsetUnit {

        Unset = Unit.Unset,
        Pixel = Unit.Pixel,
        Em = Unit.Em,
        Width = Unit.Width,
        Height = Unit.Height,
        ContentWidth = Unit.ContentWidth,
        ContentHeight = Unit.ContentHeight,
        ViewportWidth = Unit.ViewportWidth,
        ViewportHeight = Unit.ViewportHeight,
        ParentWidth = Unit.ParentWidth,
        ParentHeight = Unit.ParentHeight,
        ContentAreaWidth = Unit.ContentAreaWidth,
        ContentAreaHeight = Unit.ContentAreaHeight,
        ScreenWidth = Unit.ScreenWidth,
        ScreenHeight = Unit.ScreenHeight,
        
    }

}