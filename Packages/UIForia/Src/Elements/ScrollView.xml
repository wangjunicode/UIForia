<UITemplate>
    <Style>

        const scrollBarSize = 10px;
        const trackColor = #b2b2b2aa;

        style scrollview {
            Overflow = scroll;
        }

        style scroll-track {
            BackgroundColor = @trackColor;
            BorderRadius = 3px;
        }

        style scroll-handle {
            BorderRadius = 50%;
            BackgroundColor = grey;
        }
        
        style scroll-children {
            LayoutFitHorizontal = Grow;
        }
        
    </Style>

    <Contents style="scrollview">

        <define:Children style="scroll-children"/>

        <define:vertical-scrollbar if="verticalScrollingEnabled" style="scroll-track" mouse:click="OnClickVertical($event)"/>

        <define:vertical-scrollbar-handle if="verticalScrollingEnabled" style="scroll-handle" drag:create="OnCreateVerticalDrag($event)"/>
        
        <define:horizontal-scrollbar if="horizontalScrollingEnabled" style="scroll-track" mouse:click="OnClickHorizontal($event)"/>

        <define:horizontal-handle if="horizontalScrollingEnabled" style="scroll-handle" drag:create="OnCreateHorizontalDrag($event)"/>

        <!--        <Group mouse:click="OnClickVertical($event)" mouse:hover="OnHoverVertical($event)" attr:id="scroll-track-vertical" style="scroll-track-vertical">-->
        <!--            <Div drag:create="OnCreateVerticalDrag($evt)" attr:id="scroll-handle-vertical" style="scroll-handle-vertical"/>-->
        <!--        </Group>-->

        <!--        <Group mouse:click="OnClickHorizontal($event)" mouse:hover="OnHoverHorizontal($event)" attr:id="scroll-track-horizontal" style="scroll-track-horizontal">-->
        <!--            <Div drag:create="OnCreateHorizontalDrag($evt)" attr:id="scroll-handle-horizontal" style="scroll-handle-horizontal"/>-->
        <!--        </Group>-->

    </Contents>
</UITemplate>