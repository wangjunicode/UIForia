style red {
    BackgroundColor = red;
}

style container {
    FlexLayoutDirection = Horizontal;
    PreferredSize = 300px;
}

style <Group> {
    PreferredSize = 150px;
}

style blue {    
    BackgroundColor = blue;
    [hover] {
        BackgroundColor = yellow;
        Cursor = url("testimg/Cursor1") 15;
    }
}