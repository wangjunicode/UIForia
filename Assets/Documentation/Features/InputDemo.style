style input-container {
}

style <Label> {
    [hover] {
        Cursor = url("Cursors/hand");
    }
}

style input {
    Border = 2px;
    BorderColor = black;
    BackgroundColor = white;
    PreferredWidth = 500px;
    MarginBottom = 20px;
}