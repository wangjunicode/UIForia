<?xml version="1.0" encoding="utf-8"?>
<UITemplate>
    <Contents>
        <Group onMouseContext="OnMouseContext()" onMouseDown="OnMouseDown($event)" style.backgroundColor="blue"
               style.hover.backgroundColor="red" name="toolbar" someValue="{1 + 1}">

            Toolbar Here {time}

        </Group>

        <!--<Group x-if="{showMe}" onMouseEnter="OnMouseEnter($event)" style.backgroundColor="white" style.hover.backgroundColor="red" name="toolbar" someValue="{1 + 1}">-->

        <!--I am shown! {showMe}-->

        <!--</Group>-->
        <!--<Repeat list="{values}" as="item">-->
        <!--some text here {$item}-->
        <!--</Repeat>-->
        <!---->
        <Group style.backgroundColor="rgb(255, 0,0)">
            <Switch value="{selectedValue}">
                <Case when="1">
                    One!
                </Case>
                <Case when="2">
                    Two!
                </Case>
                <Default>
                    Other! {selectedValue}
                </Default>
            </Switch>
        </Group>
    </Contents>
</UITemplate>