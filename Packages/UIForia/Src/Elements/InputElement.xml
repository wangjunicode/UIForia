<?xml version="1.0" encoding="utf-8"?>
<UITemplate>

    <Style>

        style &lt;this&gt; {
            Border = 2px;
            BorderColor = black;
            BackgroundColor = white;
            Padding = 8px 8px 1px;
            PreferredWidth = 500px;
            TextFontSize = 1.2em;
            MarginBottom = 20px;
        }

        style input-element-text {
            Painter = "none";
            PreferredSize = 1pca, 1.15em;
        }

        style placeholder {
        }
        
        style placeholder-slot {
            LayoutBehavior = Ignored;
            AnchorTarget = ParentContentArea;
            TransformBehavior = AnchorMinOffset;
            TextColor = rgb(150, 150, 150);
        }

    </Style>

    <Contents style="input-element">

        <Group style="input-element-text"/>
        
        <Slot name="placeholder" style="placeholder-slot" if="ShowPlaceholder">
            <Text style="placeholder">{placeholder}</Text>
        </Slot>

    </Contents>

</UITemplate>

